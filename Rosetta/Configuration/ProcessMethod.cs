namespace Rosetta.Configuration
{
	public enum ProcessMethod
	{
		Add,
		Subtract,
		Multiply,
		Divide,
		Remainder,
		And,
		Or,
		Xor,
		ShiftLeft,
		ShiftRight,
		Trim,
		TrimLeft,
		TrimRight,
		PadLeft,
		PadRight,
		UpperCase,
		LowerCase,
		TitleCase
	}
}